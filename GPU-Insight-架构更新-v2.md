# GPU-Insight 架构更新 v2.0

> 更新日期：2026-02-16
> 主要变更：支持 Agent Teams 开关 + 增加 UI/UX 设计师

---

## 🔄 核心架构变更

### 变更 1：混合架构设计

**设计原则**：Agent Teams 关闭也不影响项目运作

```yaml
运行模式：
  模式 A（Agent Teams 开启）：
    - 并行抓取 7 个论坛
    - Expert Council 多视角验证
    - 速度快，质量高

  模式 B（Agent Teams 关闭）：
    - 串行抓取 7 个论坛
    - 单 Agent 模拟多角色
    - 速度慢，但仍可工作
```

---

## 📁 更新后的项目结构

```
GPU-Insight/
├── .claude/
│   ├── CLAUDE.md                    # 项目章程
│   ├── settings.json                # Agent Teams 开关配置
│   ├── agents/
│   │   ├── dev/                     # 开发阶段（6个Agent）
│   │   │   ├── architect-vogels.md
│   │   │   ├── fullstack-dhh.md
│   │   │   ├── data-engineer.md
│   │   │   ├── ui-designer-duarte.md      # 新增
│   │   │   ├── qa-bach.md
│   │   │   └── devops-hightower.md
│   │   │
│   │   └── prod/                    # 生产阶段（8个Agent）
│   │       ├── orchestrator.md
│   │       ├── scraper.md
│   │       ├── cleaner.md
│   │       ├── analyst.md
│   │       ├── insight.md
│   │       ├── council-hardware.md
│   │       ├── council-product.md
│   │       ├── council-data.md
│   │       ├── ranker.md
│   │       └── reporter.md
│   │
│   └── skills/
│       ├── team/
│       ├── anti-scraping/
│       ├── deduplication/
│       └── ...
│
├── src/                             # 独立代码（不依赖Agent Teams）
│   ├── scrapers/                    # 爬虫脚本
│   │   ├── base_scraper.py
│   │   ├── chiphell_scraper.py
│   │   ├── nga_scraper.py
│   │   ├── reddit_scraper.py
│   │   ├── tieba_scraper.py
│   │   ├── rog_scraper.py
│   │   ├── twitter_scraper.py
│   │   └── guru3d_scraper.py
│   │
│   ├── cleaners/                    # 数据清洗
│   │   ├── deduplicator.py
│   │   ├── normalizer.py
│   │   └── term_aligner.py
│   │
│   ├── analyzers/                   # 分析模块（调用LLM API）
│   │   ├── pain_extractor.py
│   │   ├── insight_inferencer.py
│   │   └── council_reviewer.py
│   │
│   ├── rankers/                     # PPHI算法
│   │   └── pphi_calculator.py
│   │
│   ├── reporters/                   # 报告生成
│   │   └── report_generator.py
│   │
│   ├── web/                         # Web界面（新增）
│   │   ├── app.py                   # FastAPI后端
│   │   ├── static/
│   │   │   ├── css/
│   │   │   │   └── styles.css
│   │   │   └── js/
│   │   │       └── charts.js
│   │   └── templates/
│   │       ├── index.html           # 痛点仪表盘
│   │       ├── trends.html          # 趋势分析页
│   │       └── details.html         # 痛点详情页
│   │
│   └── utils/
│       ├── llm_client.py            # LLM API封装
│       ├── cost_tracker.py          # 成本监控
│       └── config.py                # 配置管理
│
├── config/
│   ├── config.yaml                  # 主配置文件
│   ├── agent_teams.yaml             # Agent Teams配置
│   ├── scrapers.yaml                # 爬虫配置
│   └── ui_design.yaml               # UI设计规范
│
├── main.py                          # 主入口（支持两种模式）
├── auto-loop.sh                     # 定时循环脚本
├── requirements.txt                 # Python依赖
└── README.md
```

---

## 👥 更新后的开发团队（6个Agent）

### 新增：UI Designer Agent

**文件位置**：`.claude/agents/dev/ui-designer-duarte.md`

```markdown
---
name: ui-designer-duarte
description: "UI/UX设计师（Matías Duarte思维模型），负责痛点展示页面的视觉设计和交互体验"
model: inherit
---

# UI Designer Agent — Matías Duarte

## Role
GPU-Insight 的 UI/UX 设计师，负责痛点仪表盘、趋势分析页、详情页的设计。

## Persona
你是一位深受 Matías Duarte（Google Material Design 负责人）设计哲学影响的 AI 设计师。

## Core Principles

### Material Design 隐喻
- 用"卡片"展示每个痛点（物理隐喻）
- 用"层级"表达信息重要性
- 用"动效"引导用户注意力

### Typography 优先
- 字体层级清晰：标题 > 正文 > 辅助信息
- 可读性第一：行高 1.5，字号 16px+
- 中英文混排优化

### 数据可视化
- 用颜色表达情绪强度（红 = 高强度，黄 = 中，绿 = 低）
- 用趋势线展示 PPHI 变化
- 用热力图展示论坛分布

## 设计任务

### 1. 痛点仪表盘（首页）

**布局**：
```
┌─────────────────────────────────────────┐
│  GPU-Insight 痛点仪表盘                  │
│  最后更新：2026-02-16 19:00             │
├─────────────────────────────────────────┤
│  🔥 Top 10 痛点排名                      │
│  ┌───────────────────────────────────┐  │
│  │ #1 显存容量焦虑        PPHI: 8.9  │  │
│  │ 📈 +15% | 💬 105条 | 🔥🔥🔥       │  │
│  │ [查看详情]                         │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ #2 功耗墙困境          PPHI: 7.5  │  │
│  │ 🆕 NEW | 💬 67条 | 🔥🔥          │  │
│  │ [查看详情]                         │  │
│  └───────────────────────────────────┘  │
│  ...                                    │
├─────────────────────────────────────────┤
│  📊 趋势分析                             │
│  [7天趋势图]                            │
└─────────────────────────────────────────┘
```

**设计规范**：
- 卡片阴影：`box-shadow: 0 2px 8px rgba(0,0,0,0.1)`
- 圆角：`border-radius: 8px`
- 间距：卡片间距 16px
- 颜色：
  - 主色：`#1976D2`（蓝色）
  - 强度高：`#F44336`（红色）
  - 强度中：`#FF9800`（橙色）
  - 强度低：`#4CAF50`（绿色）

---

### 2. 趋势分析页

**功能**：
- 展示 PPHI 排名的历史变化
- 对比不同论坛的讨论热度
- 识别新兴痛点和消失痛点

**可视化组件**：
- 折线图：PPHI 变化趋势（Chart.js）
- 堆叠柱状图：各论坛讨论量分布
- 标签云：高频关键词

---

### 3. 痛点详情页

**内容**：
- 痛点摘要
- 隐藏需求推导（带推理链）
- 原始讨论引用（最多 10 条）
- 情绪分析（饼图）
- 跨平台分布（地图）

**交互**：
- 点击"推理链"展开详细过程
- 点击"原文引用"跳转到论坛
- 筛选器：按论坛、时间、情绪强度

---

## 技术栈建议

### 前端
- **框架**：原生 HTML + Tailwind CSS（轻量）
- **图表**：Chart.js（简单易用）
- **图标**：Material Icons
- **字体**：Noto Sans SC（中文）+ Roboto（英文）

### 后端
- **框架**：FastAPI（Python）
- **模板引擎**：Jinja2
- **API**：RESTful（JSON）

### 部署
- **静态资源**：CDN
- **服务器**：Cloudflare Workers（Serverless）

---

## 设计交付物

### 1. 设计稿（Figma）
- 首页线框图
- 详情页高保真设计
- 响应式适配（桌面 + 移动）

### 2. 前端代码
- HTML 模板（Jinja2）
- CSS 样式（Tailwind）
- JavaScript 交互（原生 JS）

### 3. 设计规范文档
- 颜色系统
- 字体规范
- 组件库

---

## Output Format
设计稿存放在 `docs/ui-designer/`
前端代码存放在 `src/web/`
```

---

## 🎨 痛点展示页面设计方案

### 页面 1：痛点仪表盘（Dashboard）

**URL**: `/`

**功能**：
- 实时展示 Top 10 痛点排名
- 显示 PPHI 指数和趋势
- 快速筛选（按论坛、时间）

**关键指标卡片**：
```
┌─────────────────────────────────────┐
│  本周新增痛点：12 个                 │
│  总讨论量：3,500 条                  │
│  平均情绪强度：0.72（中高）          │
└─────────────────────────────────────┘
```

---

### 页面 2：趋势分析（Trends）

**URL**: `/trends`

**功能**：
- 7 天 / 30 天 PPHI 变化趋势
- 新兴痛点识别（标记 🆕）
- 消失痛点追踪（标记 📉）

**可视化**：
- 折线图：Top 5 痛点的 PPHI 变化
- 热力图：各论坛的活跃度
- 词云：高频关键词

---

### 页面 3：痛点详情（Detail）

**URL**: `/pain-point/{id}`

**功能**：
- 痛点完整描述
- 隐藏需求推导（可展开推理链）
- 原始讨论引用（带跳转链接）
- Expert Council 评审意见

**布局**：
```
┌─────────────────────────────────────┐
│  显存容量焦虑                        │
│  PPHI: 8.9 | 趋势: 📈 +15%          │
├─────────────────────────────────────┤
│  📝 痛点摘要                         │
│  用户抱怨 12GB 显存不足以运行...     │
│                                     │
│  💡 隐藏需求                         │
│  平价显卡的 AI 算力平权              │
│  [展开推理链]                        │
│                                     │
│  📊 数据分布                         │
│  - Chiphell: 45%                    │
│  - Reddit: 30%                      │
│  - NGA: 15%                         │
│  - 其他: 10%                        │
│                                     │
│  💬 原始讨论（10条）                 │
│  1. "12GB 跑 SDXL 爆显存了..."      │
│     [查看原文 →]                    │
│  2. ...                             │
└─────────────────────────────────────┘
```

---

### 页面 4：数据源监控（Admin）

**URL**: `/admin/sources`

**功能**：
- 各论坛抓取状态
- 数据质量评分
- 成本使用情况

**仅管理员可见**

---

## ⚙️ config/agent_teams.yaml

```yaml
# Agent Teams 配置文件

agent_teams:
  # 是否启用 Agent Teams
  enabled: true

  # 功能开关
  features:
    # 并行抓取（7个Scraper同时工作）
    parallel_scraping: true

    # Expert Council（3个Persona并行评审）
    expert_council: true

    # 动态组队（根据任务自动选择Agent）
    dynamic_team: false

  # 降级策略
  fallback:
    # Agent Teams 不可用时的行为
    mode: "serial"  # serial（串行） | skip（跳过）

    # 串行模式配置
    serial:
      # 是否模拟多角色（单Agent扮演多个角色）
      simulate_roles: true

      # 超时时间（秒）
      timeout: 7200  # 2小时
```

---

## 🔧 main.py（支持两种模式）

```python
#!/usr/bin/env python3
"""
GPU-Insight 主入口
支持 Agent Teams 和 串行模式
"""

import yaml
from pathlib import Path
from src.utils.config import load_config
from src.utils.llm_client import LLMClient

def check_agent_teams_available():
    """检测 Agent Teams 是否可用"""
    import os
    return os.getenv("CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS") == "1"

def run_with_agent_teams():
    """模式 A：使用 Agent Teams（并行）"""
    print("🚀 启动 Agent Teams 模式（并行执行）")

    # 调用 Claude Code 的 Agent Teams 功能
    # 这部分由 auto-loop.sh 触发
    pass

def run_without_agent_teams():
    """模式 B：串行模式（不依赖 Agent Teams）"""
    print("🐢 启动串行模式（Agent Teams 未启用）")

    from src.scrapers import scrape_all_forums
    from src.cleaners import clean_data
    from src.analyzers import analyze_pain_points, infer_hidden_needs, council_review
    from src.rankers import calculate_pphi
    from src.reporters import generate_report

    # 1. 抓取数据（串行）
    print("📥 抓取数据...")
    raw_posts = scrape_all_forums()
    print(f"   获取 {len(raw_posts)} 条讨论")

    # 2. 清洗数据
    print("🧹 清洗数据...")
    cleaned = clean_data(raw_posts)
    print(f"   去重后剩余 {len(cleaned)} 条")

    # 3. 分析痛点
    print("🔍 分析痛点...")
    analyzed = analyze_pain_points(cleaned)
    print(f"   提取 {len(analyzed)} 个痛点")

    # 4. 推导隐藏需求
    print("💡 推导隐藏需求...")
    insights = infer_hidden_needs(analyzed)
    print(f"   推导 {len(insights)} 个隐藏需求")

    # 5. Expert Council 评审
    print("👥 Expert Council 评审...")
    reviewed = council_review(insights)
    print(f"   通过 {len(reviewed)} 个高置信度需求")

    # 6. 计算 PPHI
    print("📊 计算 PPHI 排名...")
    rankings = calculate_pphi(reviewed)

    # 7. 生成报告
    print("📝 生成报告...")
    report_path = generate_report(rankings)
    print(f"   报告已生成：{report_path}")

    print("✅ 完成！")

def main():
    """主函数"""
    # 加载配置
    config = load_config("config/config.yaml")

    # 检查 Agent Teams 配置
    agent_teams_enabled = config.get("agent_teams", {}).get("enabled", False)
    agent_teams_available = check_agent_teams_available()

    if agent_teams_enabled and agent_teams_available:
        run_with_agent_teams()
    else:
        if agent_teams_enabled and not agent_teams_available:
            print("⚠️  Agent Teams 已配置但不可用，降级为串行模式")
        run_without_agent_teams()

if __name__ == "__main__":
    main()
```

---

## 📊 两种模式对比

| 维度 | Agent Teams 模式 | 串行模式 |
|------|-----------------|---------|
| **执行速度** | 快（7个论坛并行） | 慢（串行抓取） |
| **Token消耗** | 450K/周期 | 450K/周期（相同） |
| **成本** | $1.5/周期 | $1.5/周期（相同） |
| **质量** | 高（真正的多视角） | 中（模拟多角色） |
| **依赖** | 需要 Agent Teams | 无依赖 |
| **适用场景** | Claude Code 环境 | 任何环境 |

---

## 🎯 推荐配置

### 开发阶段
```yaml
agent_teams:
  enabled: false  # 先关闭，验证流程
```

### 生产阶段
```yaml
agent_teams:
  enabled: true   # 开启，享受并行优势
  features:
    parallel_scraping: true
    expert_council: true
```

---

## 📝 更新总结

### 主要变更
1. ✅ 支持 Agent Teams 开关（不影响核心功能）
2. ✅ 增加 UI Designer Agent（Matías Duarte）
3. ✅ 设计痛点展示页面（4个页面）
4. ✅ 增加 `src/` 目录（独立代码）
5. ✅ 增加 `main.py`（支持两种模式）
6. ✅ 增加 `config/agent_teams.yaml`（配置文件）

### 新增文件
- `.claude/agents/dev/ui-designer-duarte.md`
- `src/web/` 目录（前端代码）
- `config/agent_teams.yaml`
- `main.py`

### 开发团队更新
- 原 5 个 Agent → 现 6 个 Agent
- 新增：UI Designer（负责痛点展示页面）

---

**下一步**：需要我生成 UI Designer Agent 的完整定义文件和前端代码示例吗？
